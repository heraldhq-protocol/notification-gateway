# ─────────────────────────────────────────────────────────────────────
# Herald Notification Gateway — Local Development Stack
# ─────────────────────────────────────────────────────────────────────
name: herald-gateway-dev
# Usage: docker compose -f docker/docker-compose.dev.yml up -d
# ─────────────────────────────────────────────────────────────────────
# Starts Redis + the API gateway + the background worker.
# Postgres is expected to already be running on the host.
# ─────────────────────────────────────────────────────────────────────

services:
  # ── Redis ──────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: herald-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - herald_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - herald-gateway-dev-net

  # ── API Gateway (hot-reload via nest start --watch) ────────────────
  gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
      network: host
    container_name: herald-gateway
    entrypoint: ["/bin/sh", "/app/docker/entrypoint.sh"]
    command: ["gateway"]
    ports:
      - "3002:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
      - CI=true
      - TSC_WATCHFILE=DynamicPriorityPolling
      - DATABASE_URL=postgresql://postgres:admin@host.docker.internal:5432/herald_gateway?schema=public&sslmode=disable
      - REDIS_URL=redis://redis:6379
    env_file:
      - ../.env
    volumes:
      - ..:/app
      - /app/node_modules
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - herald-gateway-dev-net
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ── Background Worker (restarts when dist/ changes) ────────────────
  worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
      network: host
    container_name: herald-worker
    entrypoint: ["/bin/sh", "/app/docker/entrypoint.sh"]
    command: ["worker"]
    environment:
      - NODE_ENV=development
      - CI=true
      - SERVICE_TYPE=worker
      - DATABASE_URL=postgresql://postgres:admin@host.docker.internal:5432/herald_gateway?schema=public&sslmode=disable
      - REDIS_URL=redis://redis:6379
    env_file:
      - ../.env
    volumes:
      - ..:/app
      - /app/node_modules
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - herald-gateway-dev-net
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  herald-gateway-dev-net:
    driver: bridge

volumes:
  herald_redis_data:
